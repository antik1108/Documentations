I am a DevOps-focused B.Tech student working on Linux systems.

Currently, I am using Debian 12 on an old laptop as a server. This system has:

* No GUI (no desktop environment)
* No terminal emulator (like Kitty, Alacritty, etc.)
* Only pure Linux TTY (CLI) access
* Limited hardware resources (low RAM, basic CPU)

I want to customize my terminal prompt to look clean, minimal, and professional — suitable for a DevOps/server environment.

Important constraints:

* Must work in pure TTY (no GUI features)
* No Nerd Fonts or icons
* No broken Unicode or unsupported symbols
* Must be lightweight and fast
* Should not rely on graphical features

Preferences:

* Clean minimal design
* Show username@hostname
* Show current directory
* Show Git branch (if inside a repo)
* Optional: command execution time
* Simple symbols like `>>>` or `➜` (only if supported in TTY)

I am deciding between Starship and Oh My Posh, but I prefer the best tool for TTY environments.

Please:

1. Recommend the best tool for my setup
2. Provide a complete step-by-step setup
3. Give a clean, optimized configuration
4. Ensure everything works perfectly in pure CLI without rendering issues

Avoid:

* GUI-based suggestions
* Fancy themes requiring Nerd Fonts
* Powerline or icon-heavy setups

Goal:
A clean, fast, professional terminal prompt suitable for real DevOps/server usage.

